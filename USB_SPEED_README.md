# USB U盘速度检测工具

## 📖 问题解答

### 1️⃣ 不同的U盘，插在同一个USB口上，速度会有区别吗？

**答：会有巨大区别！差异分为两个层面：**

#### 第一层：USB协议速度（总线协商速度）

USB口和U盘会协商一个速度，取决于**两者中较慢的那个**：

| USB口类型 | U盘类型 | 协商速度 | 理论最大值 |
|----------|---------|---------|-----------|
| USB 2.0  | USB 2.0 | 480 Mbps | 60 MB/s |
| USB 2.0  | USB 3.0 | 480 Mbps | 60 MB/s |
| USB 3.0  | USB 2.0 | 480 Mbps | 60 MB/s |
| USB 3.0  | USB 3.0 | 5000 Mbps | 625 MB/s |

**关键点**：USB 3.0 U盘插在USB 2.0口上，只能以USB 2.0速度运行！

#### 第二层：U盘实际性能（真实读写速度）

**即使协议速度相同，实际速度也可能相差10倍！**

同样是USB 3.0协议的U盘：
- **低端U盘**：实际读取 50-80 MB/s，写入 10-30 MB/s
- **中端U盘**：实际读取 100-150 MB/s，写入 30-60 MB/s  
- **高端U盘**：实际读取 200-400 MB/s，写入 100-200 MB/s

**影响实际速度的因素**：
1. **闪存芯片质量**
   - SLC（最快最贵）> MLC > TLC > QLC（最慢最便宜）
   
2. **控制器性能**
   - 高端控制器：多通道、高缓存
   - 低端控制器：单通道、无缓存
   
3. **是否有DRAM缓存**
   - 有缓存：小文件快，随机读写好
   - 无缓存：性能波动大
   
4. **固件优化**
   - 品牌厂商优化好
   - 杂牌厂商优化差

### 2️⃣ 可以精确获取到某个U盘的速率吗？

**答：可以！但需要分两步获取：**

#### 步骤1：获取USB协议速度（协商速度）

通过 **udev** 或 **sysfs** 可以获取USB总线协商的速度：

```bash
# 方法1：使用udevadm
udevadm info --name=/dev/sdb | grep ID_USB

# 方法2：直接读取sysfs
cat /sys/bus/usb/devices/*/speed

# 方法3：使用lsusb
lsusb -t
```

**这个速度是USB协议层面的理论最大值。**

#### 步骤2：测试实际读写速度

需要实际读写测试才能知道真实性能：

```bash
# 使用本工具测试
sudo ./usb_speed_detector /dev/sdb

# 或使用dd命令测试
sudo dd if=/dev/sdb of=/dev/null bs=1M count=1024 status=progress
```

**这个速度是U盘的真实性能。**

## 🛠️ 工具说明

本项目提供两个工具来解决上述问题：

### 1. `usb_speed_detector` - U盘速度全面检测

**功能**：
- ✅ 获取USB协议速度（USB 1.0/2.0/3.0/3.1/3.2）
- ✅ 显示理论最大传输速度
- ✅ 测试实际读取速度（需要root权限）
- ✅ 分析性能并给出评价
- ✅ 显示制造商、产品名称、序列号等详细信息

**使用方法**：
```bash
# 查看设备列表
lsblk

# 检测指定U盘（需要root权限才能测试实际速度）
sudo ./usb_speed_detector /dev/sdb
```

**输出示例**：
```
═══════════════════════════════════════════════════════
📱 USB设备信息
═══════════════════════════════════════════════════════
设备路径:     /dev/sdb
制造商:       SanDisk
产品名称:     Ultra USB 3.0
序列号:       4C530001234567890123
VID:PID:      0781:5581
───────────────────────────────────────────────────────
⚡ USB协议速度（协商速度）
═══════════════════════════════════════════════════════
USB版本:      USB 3.0/3.1 Gen1 (SuperSpeed)
协商速度:     5000 Mbps
理论最大值:   625.00 MB/s
───────────────────────────────────────────────────────
🎯 实际测试速度
═══════════════════════════════════════════════════════
实际读取速度: 145.67 MB/s
───────────────────────────────────────────────────────
📊 性能分析
═══════════════════════════════════════════════════════
协议理论速度: 625.00 MB/s
实际测试速度: 145.67 MB/s
协议利用率:   23.3%
───────────────────────────────────────────────────────
💭 评价: 中等性能U盘，适合一般使用
```

### 2. `usb_monitor` - 实时监听U盘插入

**功能**：
- ✅ 自动监听USB设备插入事件
- ✅ 实时显示设备信息和速度
- ✅ 给出性能评估建议
- ✅ 后台运行，无需手动检测

**使用方法**：
```bash
# 启动监听（推荐在后台运行）
sudo ./usb_monitor
```

**输出示例**：
```
╔═══════════════════════════════════════════════════════╗
║  🔌 检测到USB存储设备插入                             ║
╚═══════════════════════════════════════════════════════╝
⏰ 时间:       2025-10-27 14:32:15
📁 设备:       /dev/sdb
🏭 制造商:     Kingston
📦 产品:       DataTraveler 3.0
───────────────────────────────────────────────────────
⚡ USB版本:    USB 3.0/3.1 Gen1
🚀 协商速度:   5000 Mbps
📊 理论速度:   625.00 MB/s
═══════════════════════════════════════════════════════
✅ 这是USB 3.0+设备，具备高速传输能力。
   实际速度取决于U盘质量，优质U盘可达100-400 MB/s。
```

## 📦 安装和编译

### 1. 安装依赖

在Debian/Ubuntu系统上：

```bash
sudo apt-get update
sudo apt-get install -y build-essential libglib2.0-dev libudev-dev
```

在Fedora/RHEL系统上：

```bash
sudo dnf install -y gcc glib2-devel systemd-devel
```

### 2. 编译

```bash
# 使用Makefile编译
make -f Makefile.usb_tools

# 或手动编译
gcc -o usb_speed_detector usb_speed_detector.c `pkg-config --cflags --libs glib-2.0 libudev`
gcc -o usb_monitor usb_monitor.c `pkg-config --cflags --libs glib-2.0 libudev`
```

## 💡 实用技巧

### 技巧1：快速判断U盘是否插在正确的USB口上

```bash
# 检查U盘协商速度
sudo ./usb_speed_detector /dev/sdb | grep "协商速度"

# 如果显示480 Mbps但U盘是USB 3.0的，说明：
# - 可能插在了USB 2.0口上（黑色）
# - 应该插到USB 3.0口上（蓝色）
```

### 技巧2：对比不同U盘的实际性能

```bash
# 测试U盘A
sudo ./usb_speed_detector /dev/sdb > usb_a.txt

# 拔出，插入U盘B
sudo ./usb_speed_detector /dev/sdb > usb_b.txt

# 对比结果
diff usb_a.txt usb_b.txt
```

### 技巧3：购买U盘前的性能估算

| 协商速度 | 低端U盘实际速度 | 中端U盘实际速度 | 高端U盘实际速度 |
|---------|---------------|---------------|---------------|
| USB 2.0 (480 Mbps) | 15-25 MB/s | 25-35 MB/s | 35-45 MB/s |
| USB 3.0 (5000 Mbps) | 50-80 MB/s | 100-150 MB/s | 200-400 MB/s |
| USB 3.1 Gen2 (10000 Mbps) | 100-200 MB/s | 300-500 MB/s | 500-1000 MB/s |

**建议**：
- 日常使用：USB 3.0中端U盘即可
- 经常传大文件：选择高端USB 3.0或USB 3.1 U盘
- 对速度无要求：USB 2.0也够用

## 🔍 常见问题

### Q1: 为什么我的USB 3.0 U盘只跑出50MB/s？

**可能原因**：
1. ❌ 插在了USB 2.0口上（检查：如果协商速度显示480 Mbps就是这个原因）
2. ❌ U盘本身是低端产品（协商速度5000 Mbps但实际速度慢）
3. ❌ U盘几乎满了（闪存写满后速度下降）
4. ❌ 文件系统不合适（FAT32在大文件时慢，建议exFAT或NTFS）

### Q2: 同样是USB 3.0，为什么不同品牌差别这么大？

**答**：协议速度只是总线带宽，实际速度取决于：
- 闪存芯片：TLC便宜但慢，SLC贵但快
- 控制器：杂牌单通道vs品牌多通道
- 有无缓存：有DRAM缓存的小文件性能好很多
- 固件优化：大厂优化好，杂牌几乎不优化

**这就是为什么同样USB 3.0，有的50元，有的300元。**

### Q3: 如何选购高性能U盘？

**购买前检查清单**：
1. ✅ 至少USB 3.0（蓝色接口）
2. ✅ 品牌选择：三星、闪迪、金士顿等
3. ✅ 看评测：查实测读写速度，不要只看USB版本
4. ✅ 看价格：太便宜的USB 3.0大概率是低端货
5. ✅ 看标注：注意"读取速度"vs"写入速度"，写入通常慢很多

**推荐购买标准**：
- 读取 > 150 MB/s
- 写入 > 50 MB/s
- 有品牌保修

## 📚 技术原理

### USB速度协商过程

1. U盘插入
2. 内核枚举设备，读取设备描述符
3. 判断USB版本（1.0/2.0/3.0等）
4. 选择两端都支持的最高速度
5. 记录到 `/sys/bus/usb/devices/*/speed`

### 实际速度测试原理

1. 打开设备文件（如`/dev/sdb`）
2. 使用`O_DIRECT`标志绕过系统缓存
3. 读取一定量数据（本工具使用64MB）
4. 计算时间，得出速度（MB/s）

## ⚠️ 注意事项

1. **需要root权限**：实际速度测试需要直接读取设备
2. **测试会读取设备**：虽然只读不写，但请确保设备未在使用中
3. **速度波动正常**：测试结果可能有±10%的误差
4. **不要在测试时拔出U盘**：可能导致数据损坏

## 📄 许可证

本工具基于GLib示例代码修改，使用LGPL 2.1许可证。

## 🤝 贡献

欢迎提交问题和改进建议！
